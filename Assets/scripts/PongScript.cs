using UnityEngine;

public class PongScript : MonoBehaviour
{
    // public GameObject prefab;
    //
    // private float startingMovemntRight = 20f;
    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // void Start() {
    //     GameObject ball;
    //     ball = Instantiate(prefab, transform.Translate(22f,0f,0f));
    //     Vector3 force = new Vector3(startingMovemntRight, 0f, 0f);
    //     Rigidbody rBody = ball.GetComponent<Rigidbody>();
    //     rBody.AddForce(force,ForceMode.Impulse);
    // }
}
